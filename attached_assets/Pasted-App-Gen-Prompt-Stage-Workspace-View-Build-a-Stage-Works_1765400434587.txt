App Gen Prompt – Stage Workspace View

Build a “Stage Workspace View” page at route /projects/:projectId/stages/:stageId.
This is the working view for a specific stage.
Load the stage, tasks in that stage, milestones in that stage, the stage view configuration, and any contextual guidance.
Allow filtering tasks by status and assignee, and switching saved views via view_id filter if needed.
Display tasks using the configured view (e.g., Kanban/table/timeline), show milestone summary for that stage, and a guidance panel with tips/checklists.
Use the JSON view schema provided.

JSON – Stage Workspace View
{
  "id": "stage_workspace",
  "name": "Stage Workspace View",
  "route": "/projects/:projectId/stages/:stageId",
  "primary_entity": "Task",
  "data_sources": [
    {
      "id": "stage",
      "entity": "ProjectStage",
      "endpoint": "/api/projects/{{route.projectId}}/stages/{{route.stageId}}",
      "method": "GET"
    },
    {
      "id": "tasks",
      "entity": "Task",
      "endpoint": "/api/projects/{{route.projectId}}/tasks",
      "method": "GET",
      "params": {
        "stage_id": "{{route.stageId}}",
        "view_id": "{{filters.view_id}}",
        "status": "{{filters.status}}",
        "assignee_id": "{{filters.assignee_id}}"
      }
    },
    {
      "id": "milestones",
      "entity": "Milestone",
      "endpoint": "/api/projects/{{route.projectId}}/milestones",
      "method": "GET",
      "params": {
        "stage_id": "{{route.stageId}}"
      }
    },
    {
      "id": "view_config",
      "entity": "StageViewConfig",
      "endpoint": "/api/projects/{{route.projectId}}/stages/{{route.stageId}}/view-config",
      "method": "GET"
    },
    {
      "id": "guidance",
      "entity": "GuidanceItem",
      "endpoint": "/api/projects/{{route.projectId}}/guidance",
      "method": "GET",
      "params": {
        "context_type": "stage",
        "context_id": "{{route.stageId}}"
      }
    }
  ],
  "entities": {
    "Task": [
      { "id": "id", "label": "Task Id", "type": "string", "source": "id" },
      { "id": "title", "label": "Title", "type": "string", "source": "title" },
      { "id": "status", "label": "Status", "type": "enum", "source": "status" },
      { "id": "assignee_name", "label": "Assignee", "type": "string", "source": "assignee.name" },
      { "id": "due_date", "label": "Due Date", "type": "date", "source": "due_date" },
      { "id": "priority", "label": "Priority", "type": "enum", "source": "priority" },
      { "id": "milestone_name", "label": "Milestone", "type": "string", "source": "milestone.name" }
    ],
    "ProjectStage": [
      { "id": "id", "label": "Stage Id", "type": "string", "source": "id" },
      { "id": "name", "label": "Stage Name", "type": "string", "source": "name" }
    ],
    "Milestone": [
      { "id": "id", "label": "Milestone Id", "type": "string", "source": "id" },
      { "id": "name", "label": "Milestone Name", "type": "string", "source": "name" },
      { "id": "target_date", "label": "Target Date", "type": "date", "source": "target_date" },
      { "id": "progress_percent", "label": "Progress %", "type": "number", "source": "progress_percent" }
    ],
    "StageViewConfig": [
      { "id": "id", "label": "Config Id", "type": "string", "source": "id" },
      { "id": "view_type", "label": "View Type", "type": "enum", "source": "view_type" },
      { "id": "visible_fields", "label": "Visible Fields", "type": "array", "items_type": "string", "source": "visible_fields" }
    ],
    "GuidanceItem": [
      { "id": "id", "label": "Guidance Id", "type": "string", "source": "id" },
      { "id": "title", "label": "Title", "type": "string", "source": "title" },
      { "id": "body", "label": "Body", "type": "string", "source": "body" },
      { "id": "priority", "label": "Priority", "type": "enum", "source": "priority" }
    ]
  },
  "filters": [
    {
      "id": "view_id",
      "label": "Saved View",
      "type": "reference",
      "field": "SavedView.id",
      "operator": "eq",
      "reference_entity": "SavedView"
    },
    {
      "id": "status",
      "label": "Status",
      "type": "enum",
      "field": "Task.status",
      "operator": "in"
    },
    {
      "id": "assignee_id",
      "label": "Assignee",
      "type": "reference",
      "field": "Task.assignee_id",
      "operator": "eq",
      "reference_entity": "User"
    }
  ],
  "actions": [
    {
      "id": "create_task",
      "label": "Create Task",
      "type": "mutation",
      "endpoint": "/api/projects/{{route.projectId}}/tasks",
      "method": "POST"
    },
    {
      "id": "update_task",
      "label": "Update Task",
      "type": "mutation",
      "endpoint": "/api/projects/{{route.projectId}}/tasks/{{Task.id}}",
      "method": "PUT"
    },
    {
      "id": "open_task_detail",
      "label": "Open Task Detail",
      "type": "navigation",
      "target_route": "/projects/{{route.projectId}}/tasks/{{Task.id}}"
    }
  ],
  "parameters": [],
  "state": {}
}
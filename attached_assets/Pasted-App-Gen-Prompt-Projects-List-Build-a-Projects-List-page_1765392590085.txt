App Gen Prompt – Projects List

Build a “Projects List” page at route /projects.
The page should show a table of projects with search, filters, pagination, and actions.
Each row should show: project name, client, status, phase, owner, start/end dates, risk level, and % complete.
Include filters for status, owner, risk, and phase.
Add buttons: “Create Project”, “Import Project From File”.
Clicking a row or an “Open” action should navigate to /projects/{id}.
Use the JSON view schema provided to wire up data, filters, and actions.

JSON – Projects List
{
  "id": "projects_list",
  "name": "Projects List",
  "route": "/projects",
  "primary_entity": "Project",
  "data_sources": [
    {
      "id": "projects",
      "entity": "Project",
      "endpoint": "/api/projects",
      "method": "GET",
      "params": {
        "search": "{{filters.search}}",
        "status": "{{filters.status}}",
        "owner_id": "{{filters.owner_id}}",
        "risk_level": "{{filters.risk_level}}",
        "phase": "{{filters.phase}}",
        "page": "{{state.pagination.page}}",
        "page_size": "{{state.pagination.page_size}}"
      }
    }
  ],
  "entities": {
    "Project": [
      { "id": "id", "label": "ID", "type": "string", "source": "id" },
      { "id": "name", "label": "Project Name", "type": "string", "source": "name" },
      { "id": "client_name", "label": "Client", "type": "string", "source": "client.name" },
      { "id": "status", "label": "Status", "type": "string", "source": "status" },
      { "id": "phase", "label": "Phase", "type": "string", "source": "phase" },
      { "id": "owner_id", "label": "Owner Id", "type": "string", "source": "owner.id" },
      { "id": "owner_name", "label": "Owner", "type": "string", "source": "owner.name" },
      { "id": "start_date", "label": "Start Date", "type": "date", "source": "start_date" },
      { "id": "end_date", "label": "End Date", "type": "date", "source": "end_date" },
      { "id": "risk_level", "label": "Risk", "type": "string", "source": "risk_level" },
      { "id": "progress_percent", "label": "Progress %", "type": "number", "source": "progress_percent" }
    ]
  },
  "filters": [
    { "id": "search", "label": "Search", "type": "string", "field": "Project.name", "operator": "contains" },
    { "id": "status", "label": "Status", "type": "enum", "field": "Project.status", "operator": "in" },
    { "id": "owner_id", "label": "Owner", "type": "reference", "field": "Project.owner_id", "operator": "eq", "reference_entity": "User" },
    { "id": "risk_level", "label": "Risk", "type": "enum", "field": "Project.risk_level", "operator": "in" },
    { "id": "phase", "label": "Phase", "type": "enum", "field": "Project.phase", "operator": "in" }
  ],
  "actions": [
    { "id": "create_project", "label": "Create Project", "type": "navigation", "target_route": "/projects/new" },
    { "id": "import_project", "label": "Import Project From File", "type": "navigation", "target_route": "/projects/import" },
    { "id": "open_project_overview", "label": "Open Project Overview", "type": "navigation", "target_route": "/projects/{{row.id}}" }
  ],
  "parameters": [],
  "state": {
    "pagination": {
      "page": 1,
      "page_size": 25
    },
    "sort": {
      "field": "name",
      "direction": "asc"
    }
  }
}